Currently, date column isn't implemented and the values are ints
Show loading icons over each tab while waiting for data
Excel export isn't wired up, 2nd parameter isn't defined in the js port 
Test header search
Table header items are jumbled up a bit
Account Details page content is a bit small
